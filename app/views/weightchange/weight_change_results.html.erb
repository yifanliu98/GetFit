<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">

    <nav>
      <ul class="nav navbar-nav navbar-left">
        <li><img class="mb-4" src="logo.png" alt="logo" width="72" height="72"></li>
        <li><%= link_to "Todaysplan", "/todo_lists" %></li>
        <li><%= link_to "Daily Calorie Intake","/analyzeweightchange" %></li>
        <li><%= link_to "Nearby Gyms", "/nearbygyms" %></li>
        <li><%= link_to "Stopwatch", "/stopwatch" %></li>
        <li><%= link_to "Home", "/home" %></li>
        <li><%= link_to "Log Out", "/logout"%></li>

      </ul>
    </nav>
  </div>
</header>
<br><br><br>

<style>
  h1 {
  text-align: center;
  }
  h2 {
  text-align: center;
  }

</style>

<body style="background-color:#87CEEB">
  <h1>Calorie Intake Results</h1> <br>
  
  <h2>To maintain your current weight you require a daily intake of</h2><br>
  <h2 id="demo"></h2>
  <h2>Calories</h2>


  <script>
  function getUrlVars() {
    var vars = {};
    var parts = window.location.href.replace(/[?&]+([^=&]+)=([^&]*)/gi, function(m,key,value) {
      vars[key] = value;
    });
    return vars;
  }


  function printResults() {
    
    var weight = getUrlVars()["Weight"];
    var height = getUrlVars()["Height"];
    var age = getUrlVars()["Age"];
    var gender = getUrlVars()["Gender"];
    var calories = getUrlVars()["CalorieMultiplier"];
    var BMR;
    var calories_per_day;
    
    if (gender = "male") {
      var BMR = 10*weight + 6.25*height -5*age + 5;
    }
    else {
      var BMR = 10*weight + 6.25*height -5*age - 161;
    }

    switch(calories) {
      case "BMR":
         calories_per_day = BMR;
        break;
      case "sedentary":
         calories_per_day = BMR*1.2;
        break;
      case "light":
         calories_per_day = BMR*1.375;
        break;
      case "moderate":
         calories_per_day = BMR*1.55;
        break;
      case "hard":
         calories_per_day = BMR*1.725;
        break;
      case "extreme":
         calories_per_day = BMR*1.9;
        break;
    }

    return calories_per_day;
  }
  document.getElementById("demo").innerHTML = printResults();
  </script>


</body>











